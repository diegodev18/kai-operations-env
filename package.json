{
  "name": "kai-operations-env",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "bun run --filter web --filter api build",
    "build:shared": "bun run --filter @kai/shared build",
    "dev": "bun run --filter web --filter api dev",
    "start": "bun run --filter web --filter api start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "shadcn": "^4.1.0"
  },
  "dependencies": {
    "remark-breaks": "^4.0.0"
  }
}
