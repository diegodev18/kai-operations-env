{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/app.ts",
    "build": "bun -e \"console.log('[api] Sin paso de build: Bun ejecuta TypeScript nativo. Usa bun run start o bun run dev.')\"",
    "start": "bun src/app.ts"
  },
  "dependencies": {
    "hono": "^4.6.11"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
