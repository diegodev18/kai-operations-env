/** Payload de grower para API y listados ligeros de agente. */
export type GrowerPayload = {
  name: string;
  email: string;
};

/** Payload de tech lead para API y listados ligeros de agente. */
export type TechLeadPayload = {
  name: string;
  email: string;
};
